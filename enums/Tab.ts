/**
 * Tab enum for defining application tabs.
 * This enum is used to reference the different tabs in the application.
 */
export enum Tab {
  Home = 'index',
  Education = 'education',
  Experience = 'experience',
  Projects = 'projects',
  Awards = 'awards',
  Skills = 'skills',
  ContactInfo = 'contact_info',
}
